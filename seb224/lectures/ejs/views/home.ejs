<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>

   <h1>We are rendering an EJS home page!</h1> 

   <% if (true) { %>
    <p>This is showing up because true</p>


   <% } %>

   <p>the sum of 2+2 is equal to <%=2+2%></p>

   <% let showMessage = false %>
   <% if (showMessage) { %>
    <p> Thisissisisisis</p>
   <% } %>
<% let favoriteNumber = 9; %>
<p> My favorite number is <%= favoriteNumber%> </p>
<h1><%= msg %>, <%= player.name %></h1>
<ul>
    <% inventory.forEach((item, index) => { %>
    <a href="/<%=index%>"><li><%= item.name %>: <%= item.qty %></li> </a>
    <% }); %>
    <%= inventory[0].name %>
  </ul>

</body>
</html>