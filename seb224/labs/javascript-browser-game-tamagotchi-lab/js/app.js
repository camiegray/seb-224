// 1) Define the required variables used to track the state of the game.

// 2) Store cached element references.

// 3) Upon loading, the game state should be initialized, and a function should
//    be called to render this game state.

// 4) The state of the game should be rendered to the user.

// 5) Handle the game over logic.

// 6) Handle each instance of a player clicking a button with the use of a
//    `handleClick()` function.

// 7) Create reset functionality.

let state = {
  boredom: 0,
  hunger: 0,
  sleepiness: 0,
};

// let timer

// let gameover
// let clickFunction = function {}

const boredomStat = state.boredom;
const hungerStat = state.hunger;
const sleepinessStat = state.sleepiness;
console.log(boredomElement);

const playBtn = document .querySelector("#play") .addEventListener("click", function (event) {
    console.log("adsfsf");
  });
const sleepBtn = document.querySelector("#sleep").addEventListener("click", function (event) {
    console.log("haha that tickles");
  });

const feedBtn = document .querySelector("#feed") .addEventListener("click", function (event) {
    console.log("rrrr");
  });

// const resetBtn = document.querySelector("#reset-btn").addEventListener("click", clickFunction) {
//     console.log()})
// const gameMesage = document.querySelector("game-message").addEventListener("click", clickFunction) {
//     console.log()})
